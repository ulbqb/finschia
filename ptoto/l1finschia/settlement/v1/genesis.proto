syntax = "proto3";
package l1finschia.settlement.v1;

import "gogoproto/gogo.proto";

option go_package = "github.com/finschia/finschia/x/settlement/types";

// GenesisState defines the settlement module's genesis state.
message GenesisState {
  // State hashes of the global start.
  bytes globalStartState = 1;
  // Address of the defender.
  string defender = 2; // ?
}
